
    </div>

    <!-- Premium Calculator Modal -->
    <div class="modal fade" id="premiumModal" tabindex="-1" aria-labelledby="premiumModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content border-0">
                <div class="modal-header text-white modal-gradient-header">
                    <h5 class="modal-title" id="premiumModalLabel"><i class="fas fa-calculator me-2"></i>Estimate Premium</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="premiumForm" class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">Age</label>
                            <input type="number" id="ageInput" class="form-control" min="18" max="75" placeholder="e.g. 30" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Coverage (₹)</label>
                            <input type="number" id="coverageInput" class="form-control" step="50000" min="100000" placeholder="e.g. 500000" required>
                        </div>
                        <div class="col-md-12">
                            <label class="form-label">Plan Type</label>
                            <select id="planType" class="form-select">
                                <option value="individual">Individual Health</option>
                                <option value="critical">Critical Illness</option>
                                <option value="accident">Personal Accident</option>
                            </select>
                        </div>
                        <div class="col-12">
                            <button type="submit" class="btn btn-primary w-100">Calculate</button>
                        </div>
                    </form>
                    <div id="premiumResult" class="mt-3 d-none">
                        <div class="alert alert-info mb-2">
                            Estimated Monthly Premium: <strong id="premiumValue">₹0</strong>
                        </div>
                        <small class="text-muted">This is a pseudo estimate for demo purposes.</small>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>